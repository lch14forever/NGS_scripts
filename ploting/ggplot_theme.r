library(ggplot2)


figtheme <- theme_bw() + theme(text = element_text(size=20,face='bold'),panel.border  = element_rect(colour = "black",size=2),axis.title.y=element_text(vjust=1),plot.title = element_text(vjust=2))
theme_set(figtheme)

